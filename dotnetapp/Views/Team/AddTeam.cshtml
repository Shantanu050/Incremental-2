@model dotnetapp.Models.Team
@using(Html.BeginForm())
{
    @Html.DisplayNameFor(m=>m.Name)
    <p></p>
    @Html.EditorFor(m=>m.Name, new { htmlAttributes = new { placeholder = "Enter Team Name" } })
    <input type="submit" class="btn btn-primary" style="display:inline-flex">
}